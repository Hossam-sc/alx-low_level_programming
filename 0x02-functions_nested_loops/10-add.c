#include "main.h"
#include <stdio.h>
#include <stdlib.h>

/**
 * add - function to print added of two integers
 * @N: number for add
 * @M: number 2 for add
 * Return: 0 Always
 */
int add(int N, int M)
{
	return (N + M);
}

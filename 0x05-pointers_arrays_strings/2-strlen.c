#include "main.h"
#include <string.h>

/**
 * _srtlen - function that returns the length of a string
 *@s: the integer
 * Return: void
 */
int _strlen(char *s)
{
	int len;

	len = _strlen(s);
	return (len);
}
